
import Head from "next/head";
const Layout = ({ children }) => {
    return (
        <>
            <Head>

            </Head>
            <header>

            </header>
            <main className="main">
                {children}
            </main>
            <footer>

            </footer>
        </>
    )
}

export default Layout;